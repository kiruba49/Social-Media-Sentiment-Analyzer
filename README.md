# Social-Media-Sentiment-Analyzer
This project uses Pandas and NumPy for sales data analytics. It covers data cleaning (imputing NaNs, correcting invalid values), feature engineering (calculating total sales), aggregation (regional revenue reports), and NumPy vectorized operations for efficient stock analysis.
